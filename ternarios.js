//SEM TERNARIO
let isValid = false; //é validado se depois do =o campo é verdadeiro ou falso 

function verify(isValid){
    if(isValid){
        return true;
    }
    else{
        return false;
    }
}
console.log(verify(isValid));

//COM TERNARIO
const result = isValid ? true : false; //NÃO É USADO ASSIM SO EXEMPLO CONST QUANDO USAOS O VAOR NÃO PODE SER MAIS ALTERADO
 
let zero = 0;

console.log(numeroResultado);

